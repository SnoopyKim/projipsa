# 2026-07 Project Log

Append factual entries in chronological order.

## [2026-07-30] update | v0.3 skill split shipped to main

- merged: pull request 1, three independent Skills replacing the v0.2 router
- added: `disable-model-invocation` for Claude Code, matching the existing
  Codex loading policy
- fixed: templates shipped `confidence: confirmed` with no sources, which the
  memory validator always rejected
- evidence: 26 tests on Python 3.12 and 3.9.6, CI green

## [2026-07-30] update | Shipped content moved under plugins/projipsa

- merged: pull request 2, 33 files moved as pure renames
- changed: package validator scoped to the shipped tree and the README
- evidence: decoy files under a scratch `docs/` tree no longer fail package
  validation, where four of them did before

## [2026-07-30] update | Root pointer validation corrected

- merged: pull request 3, three defects fixed
- fixed: a rejected `@./AGENTS.md` import, a substring match that accepted a
  block naming a different root, and a silent skip when no project root was
  found
- evidence: each fix reverted individually failed exactly one test

## [2026-07-31] init | Projipsa adopted in its own repository

- added: project-memory core under `docs/`
- added: host pointer block in the repository's root `AGENTS.md` and
  `CLAUDE.md`
- decision: wiki/decisions/2026-07-31-projipsa-adoption.md
- validation: frontmatter, IDs, links, pointer blocks, and docs-only diff
  reviewed

## [2026-07-31] update | Plugin installed from this checkout and verified

- added: `.claude-plugin/marketplace.json`, making this repository installable
  as a directory marketplace without waiting on the SnoopyDev listing
- confirmed: `claude plugin details projipsa` reports three Skills, no agents,
  no hooks, and no MCP servers
- confirmed: the installed cache holds exactly the plugin root and none of
  `docs/`, `tests/`, or `scripts/`, so the ship boundary now has an install as
  evidence rather than only reasoning
- confirmed: the cache is a version-pinned copy, so an edit under
  `plugins/projipsa/` needs a version bump plus `claude plugin update`
- fixed: the README documented a marketplace install that does not exist yet

## [2026-07-31] update | Three memory-validator gaps reproduced

- found: a pointer block naming `archive/docs/` passes validation of `docs/`
- found: a `CLAUDE.md` whose only `@AGENTS.md` sits in a fenced block passes
- found: a `related` entry naming no existing page ID passes
- recorded: wiki/questions/open-questions.md; none is fixed
- fixed: `initialization.md` claimed every template starts at `inferred`, where
  `assumption`, `question`, `risk`, and `delivery` start at `assumed`
- evidence: scratch fixture built from this memory tree, all three exit 0

## [2026-07-31] audit | projipsa-init re-run as an idempotent audit

- checked: the minimum core is complete, one adoption decision carries
  `projipsa_adoption: true`, every maintained page resolves from `index.md`,
  and no `related` ID dangles
- created: nothing; no second memory tree, no duplicate init log entry
- found: `page-types.md` states that templates ship `confidence: inferred`,
  where `assumption`, `question`, `risk`, and `delivery` ship `assumed`
- found: the Skill contract loaded into the invoking session came from a
  previously installed 0.4.0 cache, not from the 0.3.0 copy now installed, so a
  reload after a reinstall is required before the invoked contract is current
- validation: `validate_memory.py docs` passes; the working diff also carries
  earlier non-docs changes to `README.md`, `initialization.md`, and
  `.claude-plugin/marketplace.json`, none of them made by this run

## [2026-07-31] repair | page-types template confidence drift corrected

- fixed: `plugins/projipsa/skills/projipsa/references/page-types.md` now
  distinguishes the `inferred` and `assumed` template defaults
- updated: `wiki/project/current-state.md`
- validation: `validate_memory.py docs` and `scripts/validate_package.py` pass
