# 2026-08 Project Log

Append factual entries in chronological order.

## [2026-08-02] update | Host-specific Skill adapters separated

- changed: Codex now loads `skills/`; Claude Code loads `claude-skills/`
- changed: Codex and Claude Code now use separate repository marketplace
  manifests
- shared: all three host-neutral workflow bodies remain under `shared/`
- decision: wiki/decisions/2026-08-02-host-adapter-separation.md
- validation: package validator, 33 tests, both host plugin validators, and all
  three Codex Skill validators pass
- smoke: unqualified `$projipsa-init` fell through to the general memory Skill;
  `$projipsa:projipsa-init` loaded the intended adapter, initialized a temporary
  repository with docs-only changes, and passed memory validation
- smoke: Claude Code 2.1.220 loaded `claude-skills/`, exposed the three expected
  Projipsa commands, and resolved `/projipsa:projipsa-init` exactly

## [2026-08-03] fix | Duplicate Claude Code Skills removed in 0.3.1

- found: `claude plugin details projipsa` reported 6 Skills, two per public
  name, because Claude Code adds its manifest-declared Skill directory to the
  default `skills/` scan instead of replacing it
- probed: codex-cli 0.146.0 installed a plugin declaring
  `"skills": "./codex-skills/"` and listed its Skill from that path, and
  ignored a sibling `skills/` directory entirely
- changed: `plugins/projipsa/skills/` renamed to
  `plugins/projipsa/codex-skills/`, with the Codex manifest entry point,
  shared workflow links, README, and tests following it
- changed: version bumped to 0.3.1 in both host manifests
- changed: the package validator now fails on a `skills/` directory, on either
  manifest declaring `./skills/`, and on both manifests naming the same
  directory; two new tests cover those cases
- decision: wiki/decisions/2026-08-02-host-adapter-separation.md amended with
  the corrected mechanism and the probe evidence
- validation: package validator, 35 tests, memory validator, and
  `claude plugin validate --strict` pass; `claude plugin details projipsa`
  reports 3 Skills, down from 6
- validation: codex-cli resolved the installed package's Skill root to
  `0.3.1/codex-skills` and exposed the two implicit Skills, with
  `projipsa-init` hidden as designed
- installed: the user-scope Claude Code plugin was updated from 0.3.0 to
  0.3.1, and a fresh Claude Code session listed `projipsa:projipsa` and
  `projipsa:outsource` once each
